verify_os() {
  if [ "$1" = "ubuntu" ]
  then
    if [ "$2" != "16.04" ]
    then
      echo Unknown Ubuntu version: $2
      return 0
    fi
  else
    echo Unknown OS: $1
    return 0
  fi

  return 1
}
